#!/usr/bin/env python

from distutils.core import setup

setup(name='Seems',
      version='0.1',
      description='Simple testing library',
      author='jacobsharf@gmail.com',
      author_email='',
      url='',
      packages=['seems'],
)
